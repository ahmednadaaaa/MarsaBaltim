# دليل رفع واستضافة مشروع بالتيم على AWS (AWS Deployment Guide)

يرشدك هذا الدليل خطوة بخطوة للرفع والتشغيل الفعلي لمشروعك على خادم **AWS** (سواء كان **Lightsail** أو **EC2**) بنظام تشغيل **Ubuntu**.

---

## 🛠️ الخطوة الأولى: حجز وإعداد الخادم على AWS
1. قم بتسجيل الدخول إلى [لوحة تحكم AWS](https://console.aws.amazon.com/).
2. اختر خدمة **Lightsail** (موصى بها للسرعة والميزانية) أو **EC2**.
3. قم بإنشاء خادم جديد بالمواصفات التالية:
   * **نظام التشغيل (OS):** Ubuntu 24.04 LTS (أو Ubuntu 22.04 LTS).
   * **المواصفات:** 1 GB RAM / 1 vCPU (كافية تماماً للبداية).
4. **إعداد الحماية والمنح المفتوحة (Firewall / Security Group):**
   تأكد من فتح المنافذ التالية في جدار الحماية الخاص بالخادم على AWS:
   * `22 (SSH)`: للاتصال بالخادم والتحكم به.
   * `80 (HTTP)`: للوصول لشبكة الويب وتأكيد ملكية النطاق (Domain).
   * `443 (HTTPS)`: لتشغيل الموقع بشكل مشفر وآمن.

---

## 💻 الخطوة الثانية: الاتصال بالخادم وتثبيت Docker
اتصل بخادمك الجديد عبر الـ Terminal الخاص بجهاز الماك باستخدام ملف الـ Key Pair الخاص بك:
```bash
ssh -i /path/to/your-key.pem ubuntu@YOUR_AWS_PUBLIC_IP
```

بعد تسجيل الدخول بنجاح، قم بتشغيل الأوامر التالية لتحديث الخادم وتثبيت **Docker** و **Docker Compose**:
```bash
# تحديث حزم النظام
sudo apt update && sudo apt upgrade -y

# تثبيت Docker و Docker Compose
sudo apt install docker.io docker-compose -y

# تشغيل خدمة Docker وجعلها تعمل تلقائياً عند إعادة تشغيل الخادم
sudo systemctl enable docker
sudo systemctl start docker

# السماح للمستخدم ubuntu بتشغيل Docker بدون الحاجة لكتابة sudo في كل مرة
sudo usermod -aG docker ubuntu
```
*(ملاحظة: يجب تسجيل الخروج والدخول مجدداً لتفعيل صلاحية المجموعات الجديدة: `exit` ثم اتصل بالـ `ssh` مرة أخرى).*

---

## 📂 الخطوة الثالثة: نقل المشروع وإعداد البيئة
1. قم بنسخ مشروعك إلى الخادم عن طريق **Git** (موصى به):
   ```bash
   git clone <رابط-مستودع-المشروع-الخاص-بك>
   cd baltim_backend-2
   ```
   *أو بدلاً من ذلك، يمكنك استخدام أداة `scp` لنقل الملفات من الماك إلى الخادم مباشرة.*

2. قم بتهيئة ملف المتغيرات البيئية الخاص بالإنتاج:
   ```bash
   cp env.prod.example .env
   ```

3. قم بتحرير ملف `.env` لضبط البيانات الحقيقية للإنتاج:
   ```bash
   nano .env
   ```
   *قم بتغيير القيم التالية:*
   * `DJANGO_DEBUG=False`
   * `DJANGO_SECRET_KEY`: قم بتوليد مفتاح عشوائي قوي.
   * `DJANGO_ALLOWED_HOSTS`: ضع اسم الدومين الخاص بك (تم ضبطه تلقائياً لـ: `marsabaltim.com,www.marsabaltim.com`).
   * `DB_PASSWORD`: اختر كلمة مرور قوية لقاعدة البيانات.
   * `EMAIL_HOST_PASSWORD`: ضع كلمة السر الخاصة بتطبيق Gmail (App Password).

---

## 🔒 الخطوة الرابعة: تأمين الموقع بشهادة SSL مجانية (HTTPS)
لتأمين الموقع وتفعيل قفل الأمان في المتصفح وتطبيقات الجوال:

1. قم بتثبيت أداة **Certbot**:
   ```bash
   sudo apt install certbot -y
   ```
2. قم بإصدار شهادة الـ SSL المجانية لدومينك الخاص:
   ```bash
   sudo certbot certonly --standalone -d marsabaltim.com -d www.marsabaltim.com
   ```
   *(ستطلب منك الأداة إدخال بريدك الإلكتروني والموافقة على الشروط، وسيتم حفظ الشهادات تلقائياً في مسار `/etc/letsencrypt/live/marsabaltim.com/`).*

3. **التجديد التلقائي لشهادة SSL**:
   الشهادة صالحة لمدة 90 يوماً. يقوم Certbot بإعداد مهمة مجدولة لتجديدها تلقائياً. للتحقق من عمل التجديد:
   ```bash
   sudo certbot renew --dry-run
   ```

---

## 🚀 الخطوة الخامسة: التشغيل الفعلي (Go Live)
الآن كل شيء جاهز! لجعل الموقع متاحاً على الإنترنت بشكل آمن، قم بإعطاء سكريبت التشغيل صلاحية التنفيذ ثم شغله:

```bash
# إعطاء صلاحية التنفيذ لسكريبت الرفع والتشغيل
chmod +x deploy.sh

# تشغيل السكريبت الفعلي
./deploy.sh
```

سيقوم سكريبت `deploy.sh` بتنفيذ كل العمليات المعقدة بالنيابة عنك:
1. بناء الحاويات وتطبيق إعدادات الإنتاج.
2. ترحيل قاعدة البيانات (Django Migrations) بأمان وتلقائية.
3. تجميع كافة الملفات الساكنة (Static files) لتسريع تحميل واجهات الموقع عبر Nginx.
4. تنظيف وحذف مخلفات البناء السابقة للحفاظ على مساحة القرص الصلب للخادم.

---

## 📈 الأوامر المفيدة للمتابعة والصيانة اليومية:
* **لمشاهدة حالة الحاويات المشتغلة:**
  ```bash
  docker-compose -f docker-compose.prod.yml ps
  ```
* **لمتابعة سجلات الأخطاء الحية (Logs) للويب:**
  ```bash
  docker-compose -f docker-compose.prod.yml logs -f web
  ```
* **لإعادة تشغيل الخوادم كاملة:**
  ```bash
  docker-compose -f docker-compose.prod.yml restart
  ```
* **لإيقاف التشغيل مؤقتاً:**
  ```bash
  docker-compose -f docker-compose.prod.yml down
  ```
